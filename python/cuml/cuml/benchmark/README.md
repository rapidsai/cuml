# Benchmark Runner

This directory contains the cuML benchmark runner and supporting utilities for
benchmarking machine learning algorithms.

For example, to run a benchmark:

```bash
python run_benchmarks.py --dataset classification LogisticRegression
```

For complete benchmark documentation, configuration details, and additional
examples, see the [Benchmark Guide](../../../../wiki/BENCHMARK.md).