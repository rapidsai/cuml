#include "umap_c.h"

namespace ML {

	template<typename T>
	void UMAP::fit(T *X, T *y) {
		/**
		 * First, get the indices from a kNN model.
		 */


		/**
		 * Construct a fuzzy simplicial set, using:
		 *   n_neighbors
		 *   knn_indices
		 *   knn_dists
		 */

		/**
		 * Simplicial set embedding
		 */

	}


	template<typename T>
	void UMAP::transform(T *x) {

	}
}
